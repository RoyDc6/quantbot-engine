# Generated research artifacts

`synthetic_demo/` 由 `python -B -m hk_daily_selection demo` 生成。

- Markdown 摘要可作为报告格式示例；
- CSV 是可重复生成的中间数据，已在本目录的 `.gitignore` 中排除；
- `SYNTHETIC_DEMO_ONLY` 不是真实证券回测，也不是 alpha 证据。

## 综述结论

本目录只保存研究输出。重新运行 demo 可以覆盖同名合成文件；真实数据输出应
写入单独、有数据版本记录的目录。

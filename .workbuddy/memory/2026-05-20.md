# 2026-05-20 工作日志

## VP 三层锁死架构创建 16:03
- 参照 XMM Agent Lock Template，为 VP 策略建立三层锁死架构：
  - vp_core.py（只读）— 核心 VP 计算，参数全硬编码，Windows 只读权限（attrib +R）
  - signal_api.py（固定接口）— Agent 仅可调用 get_signal(df)，无法接触核心
  - simulate_trades.py（执行层）— POC跌破规则回测，含完整指标 + 批量回测 + CLI
- 关键修正 vs GTP 原版：
  - 原版用 VAL跌破退出 → 修正为 POC跌破退出（已回测确认的规则）
  - 原版回测循环从 i=0 开始，浪费 120 次无意义迭代 → 修正为从 LOOKBACK 开始
  - 原版无指标（夏普/回撤/胜率）→ 补全完整指标
  - 原版无 CLI → 增加 argparse 入口
- 文件位置：C:\Users\RoyGoode\Desktop\VolumeProfile_策略代码\

## 港股自动交易管线执行 09:35
- **结果**: ? 成功，耗时56.8s，无错误
- **市场状态**: BULL (VIX: CANDIDATE | Trend: BULL | Momentum: BULLISH)
- **信号**: 7标的 BUY:0 / SELL:4 / HOLD:2 / REDUCED:1
  - STRONG_SELL: 00388(-51.0), 03690(-44.9)
  - SELL: 00700(-32.6), 01024(-39.8)
  - REDUCED: 09618(+49.9), HOLD: 09988(+30.4), 06055(+18.4)
- **订单**: 0笔（无触发）
- **持仓**: 00700.HK 100股 pnl=+0.2%
- **账户**: 总资产1,506,841 | 现金1,460,041
- **?? 信号剧烈波动**: 5天内3次全面翻转（5/14全多->5/15全空->5/18全空->5/19全多->5/20全空），regime可能在CRAB/BULL边界震荡
- **?? LLM情绪因子API持续报错**: ERROR 400 function id a1e53ece，不影响传统因子

## czsc 缠论库调查 11:07（?? 已废弃）
- **结论**: GPT 代码的 `czsc.core`/`enums`/`signals` 均为幻觉，czsc 0.5.1 实际 API 对不上
- **Roy 决定**: 删除此方案。czsc 已 `pip uninstall`

## Volume Profile 策略开发与回测 11:48-13:28
- **完成**: 将 Volume Profile 正式纳入 `fusion_framework/volume_profile.py` 作为独立策略模块
- **核心算法**: Volume Smearing + 双指针扩张至70%值面积
- **三状态决策**: above_box->VP_BUY(conf=0.8) / inside_box->HOLD / below_box->VP_SELL

### 跨资产回测结果汇总
| 标的 | 时间范围 | 策略收益 | B&H收益 | 超额 | 夏普 | MaxDD | 暴露度 |
|:---|:---|:---:|:---:|:---:|:---:|:---:|:---:|
| 09988.HK | 2024-2026 | +29.17% | +75.23% | -46.06% | — | -14.73% | 26.2% |
| BTC-USDT | 2023-2026 | -2.15% | +165.42% | -167.57% | — | — | — |
| ETH-USDT | 2023-2026 | -18.82% | +93.52% | -112.34% | — | — | — |
| **03690.HK** | **2022-2026** | **+39.48%** | **-59.05%** | **+98.53%** | **6.28** | **-8.16%** | **10.8%** |

- **关键发现**: VP在下跌/震荡市表现优异，在强势上涨市中跑不赢纯持有
- **定性**: "空间滤波器"角色——风控优先，不追求Alpha最大化

### 奇点B — 彻底移除
- 回测证明在09988/BTC/ETH上均为净负贡献
- 从 `volume_profile.py` 和 `vp_benchmark.py` 中完全删除
- 同时删除临时数据文件（BTC_backtest_data.json, ETH_backtest_data.json, fetch_okx_vp_data.py）

### 后续方向确定
- **保持策略纯版本**，不追加阳线/放量确认条件
- 纸交易阶段收集数据，用实盘验证假突破率
- VP策略代码已保存至桌面: `C:\Users\RoyGoode\Desktop\VolumeProfile_Strategy.txt`

---

## ?????? 紧急修复: autype 数据事件（14:16-14:33）

### 灾难发现
- NVDA/TSLA 使用了 `autype=None`（不复权）数据
- NVDA 2024年6月 10:1拆股 → 价格从 $1,037 跳到 $121（虚假 -90%）
- VP 策略"正确"执行 POC 跌破出仓，但响应的是假信号
- **NVDA 回撤 -91.74% 和收益 -61.93% 是完全失真的**

### 修复操作
1. `fetch_vp_data.py` line 36: `autype=None` → `autype='qfq'`
2. `fetch_data.py` line 12: `autype=AuType.NONE` → `autype='qfq'`
3. 删除 `tmp/` 下全部 8 个 `*_backtest_data.json` 缓存
4. 重新拉取 5 个标的前复权K线（各1000条）
5. 验证: NVDA 拆股日 6/6=$120.93 → 6/7=$120.82 → 6/10=$121.72 ? 平滑无跳空

### 前复权横截面回测结果（POC跌破规则）

| 标的 | 策略收益 | B&H收益 | 超额 | 夏普 | MaxDD | 胜率 | 暴露度 |
|:---|---:|---:|---:|---:|---:|---:|---:|
| **00700** | **+109.97%** | +89.23% | **+20.74%** | **1.12** | -20.88% | **100%** | 32.7% |
| **03690** | -9.58% | -45.42% | **+35.84%** | 0.04 | -37.33% | 25% | 26.4% |
| **01810** | +88.04% | +203.68% | -115.64% | 0.75 | -32.40% | 33.3% | 45.3% |
| **NVDA** | **+248.58%** | +969.22% | -720.64% | **1.15** | **-31.74%** | 60% | 59.8% |
| **TSLA** | +33.15% | +101.43% | -68.28% | 0.40 | -43.65% | 50% | 41.1% |

### 修复前后对比（美股双雄）

| 指标 | NVDA 旧 (不复权) | NVDA 新 (前复权) | TSLA 旧 | TSLA 新 |
|:---|---:|---:|---:|---:|
| 策略收益 | -61.93% | **+248.58%** | -45.79% | **+33.15%** |
| 最大回撤 | -91.74% | **-31.74%** | -68.79% | **-43.65%** |
| 胜率 | 50% | **60%** | 50% | **50%** |
| 最大亏损 | -88.28% | **-9.14%** | -51.25% | **-18.51%** |

### 关键教训
- **NVDA/TSLA等有拆股历史的标的，必须使用前复权数据**
- `autype='qfq'` 在 Futu API 中用字符串即可，不需要 `AuType.QFQ` 枚举
- 港股标的（00700/03690/01810）在前复权数据下结果与之前一致（无拆股事件）
- `fetch_vp_data.py` 和 `fetch_data.py` 均已永久修复
- MEMORY.md 已同步更新：Futu API 部分用 `autype='qfq'` 替换了原来的 `autype=None`，并增加了 NVDA 案例警示

## POC规则 vs HOLD规则对比回测（14:57）
- **结论**: POC规则在全部5标的上全面优于HOLD规则
  - 00700: POC +109.97% vs HOLD +26.17%（+83.80%）
  - NVDA: POC +248.58% vs HOLD +123.00%（+125.58%）
  - 胜率: POC规则在所有标的上均大幅领先（60% vs 31.2% on NVDA）
  - 交易次数: POC减少50-70%，避免了频繁假突破进出
- **根因**: HOLD规则退出太宽松（close ≤ VAH就退），导致大量微利交易+高频率回场；POC价格在价值区内部，提供了更合理的退出缓冲区
- **POC跌破规则确认是正确版本**，维持现状
- **VP 备份包完成**：C:\Users\RoyGoode\Desktop\VolumeProfile_策略代码\ 下7个文件（vp_core.py只读 + signal_api.py + simulate_trades.py + fetch_vp_data.txt + 回测报告.txt + 说明.txt + vp_backtest_report.json），三层锁死架构（XMM Lock Template），signal_api.py 已修正缺失 CONF_BUY import 的 bug

## Volume Profile 替换缠论 Skill（16:44）
- 将 chan-theory skill 的 SKILL.md 全文重写为 Volume Profile 策略内容
- 删除旧 references/ 和 scripts/ 目录（缠论残余代码和文档）
- Skill 新名称: `volume-profile`，description 覆盖 5 类触发场景
- 内容包括：核心算法、三层锁死架构、FusionEngine 接入、回测数据（跨资产横截面 5 标的前复权结果）、POC vs HOLD 对比、关键约束

## LLM情绪因子API故障修复（17:30）
- **根因**: mistralai/mixtral-8x7b-instruct-v0.1 返回 JSON 键名含转义下划线 `sentiment\_score`，`json.loads` 报 `Invalid \escape`，所有 fallback 无法提取
- **修复**: `event_detector.py` line 478 添加 `text = text.replace('\\_', '_')` 预归一化
- **验证**: 修复后 `sentiment_score=10.0` 正常返回，宏观叙事分析 `risk_on score=30`
- **教训**: 模型输入 prompt 要求严格JSON，但模型仍会在键名中加转义；`_parse_json` 需要防御性前置归一化

## LLM情绪因子 — 多目标综合验证（17:35）
**3/3 全部通过，0 错误**
- **Sentiment Detection (4/4 PASS)**:
  - 00700.HK BULL → score=+10.0, "moderately bullish"
  - 09988.HK CRAB → score=-80.0, "strong bearish, 9 consecutive down days"
  - SPY.US CORRECTION → score=-80.0, "Bearish correction, 8 consecutive down days"
  - BTC BULL → score=+50.0, "BTC bull market with steady upward momentum"
- **Factor Scoring (2/2 PASS)**: 00700/HK signal=-41.5, SPY/SPY signal=-41.5（7因子全正常）
- **Narrative Analysis**: sentiment_score=-80.0, 无JSON解析错误
- **结论**: 修复稳定，跨标的、跨市场状态全部恢复

## SQLite Schema Migration 修复（19:41）
- **根因**: `CREATE TABLE IF NOT EXISTS` 不会修改已有表结构。早期 `quant.db` 在 `xmm_signal`/`xmm_confidence` 等字段加入 schema 之前就已创建，INSERT 时报 `table signals has no column named xmm_signal`
- **修复**: `core/quant_db.py` 的 `_init_tables()` 在 signals 表 CREATE 后增加 PRAGMA 反射+动态 ALTER TABLE 迁移
  - `PRAGMA table_info(signals)` → 获取已有字段集合
  - 对比规范 schema（31个必需字段）+ 可选扩展字段（`chanlun_structure`, `vp_signal`）
  - `ALTER TABLE signals ADD COLUMN` 补齐缺失字段
  - 异常捕获，不影响其他表初始化
- **验证结果**: 自动检测到 4 个缺失字段并全部补齐
  - ? `xmm_signal` (TEXT), `xmm_confidence` (REAL)
  - ? `chanlun_structure` (TEXT), `vp_signal` (TEXT)
  - ? 写入测试 (xmm_signal='HOLD') 成功，查询验证通过
- **文件**: `E:\quant\core\quant_db.py` 行 93-141（migration block）

## FusionEngine 命名标准化重构: event_score → event_sentiment_score（17:29）
- **背景**: DB列名和DecisionSignal字段已使用 `event_sentiment_score`，但FusionEngine.fuse()的参数名仍为 `event_score`（少了 `_sentiment`）
- **重构范围**: 3文件7处修改，零遗漏
  - `fusion_engine.py`: 5处 — fuse()签名、docstring、内部_compute_score()调用、_compute_score()签名、加权计算
  - `signal_engine.py`: 1处 — fuse()调用keyword参数
  - `daily_runner.py`: 1处 — fuse()调用keyword参数
- **验证结果**:
  - TEST 1: event_sentiment_score=0.0 → HOLD, score=-25.0 ??
  - TEST 2: event_sentiment_score=0.8 → STRONG_BUY, score=25.0 (含情绪摘要) ??
  - TEST 3: 位置参数0.6 → STRONG_SELL, score=-15.0（向后兼容）??
  - 0 TypeError, 0 异常 ??
- **命名链完全对齐**: `sentiment_score`(LLM) → `event_sentiment_score`(DB/DecisionSignal/FusionEngine) → 全线一致
# 2026-05-27 工作日志

## 重大修复：Futu K线数据管道翻页BUG

**发现过程**: HK扫描信号异常（XMM全0、REDUCED泛滥）→ 大模型指出K线数据滞后17个月 → 溯源确认 `page_key` 被 `_` 丢弃

**根因**:
1. `core/futu_adapter.py` → `request_history_kline` 的 `page_key` 返回值被 `_` 丢弃，翻页失效 → 仅取到最老的252根K线
2. `scanner2/hk_tech_scan.py` → 同款BUG + `autype=None`（不复权）
3. Futu api v10.5.6508 参数名为 `page_req_key` 不是 `page_key` → 导致线程内异常被 `except: pass` 吞噬

**修复内容**:
- `core/futu_adapter.py`: 翻页循环 + `page_req_key` 参数 + 去线程化（线程层导致不稳定）+ `max_count=252`
- `scanner2/hk_tech_scan.py`: 同上 + `autype='qfq'` 前复权

**修复后数据对比**:
- 00700: 旧 $363.6 (2025-01-09) → 新 $435.60 (2026-05-27) ✅
- AAPL: 旧 $248.83 (2024-12-31) → 新 $308.33 (2026-05-26) ✅
- NVDA: 旧数据 → 新 $214.86 (2026-05-26) ✅

**⚠️ 发现的新问题/注意事项**:
- OpenD 连续多次创建连接后可能挂起 → 需要重启
- `max_count` > 252 会导致 OpenD 无响应
- `US.AAPL` 不是标准符号格式（标准格式是 `AAPL.US`）

## 复测确认 (10:38-11:26)
- **11:23 数据已恢复** — Futu K线最后日期回到2026-05-27，翻页BUG修复生效
- **XMM=0确认是市场真实信号** — 港股6/7只DOWN趋势，价格<短底<长底，无MACD结构
- 00388 TD=9已触发但无底结构配合，01024 TD=8接近TD9
- 美股扫描(11:26): 15只，BUY:2(AMD/QCOM+30.5<40)，REDUCED:7，HOLD:6
- AAPL持仓739股+15.1%，XMM美股同样全线0

## 数据新鲜度检测讨论 (11:34)
- Roy 提出 6 点分析解释 BUG 为何潜伏 17 个月，全部命中
- 核心：没有数据新鲜度检测，系统对数据源无条件信任
- 建议方案：3层防线（3天warning日常监控 + 30天error兜底 + TickFlow回退自愈）
- 30天阈值太宽松，日K线应设3天（覆盖周末+假期）
- 不仅warning，FusionController层应消费stale标记降权/强制HOLD

## 数据新鲜度检测系统 — 已实现上线 (11:37)

三层防线代码实现：
1. **`core/futu_adapter.py`**: `fetch_kline` 返回前检查 stale_days → 写入 `df.attrs`（>30标记stale_critical, >3标记stale_warning）
2. **`core/fusion_controller.py`**: `_fetch_kline` 打印滞后告警；`analyze_ticker` 中 >30天强制HOLD+error，3-30天置信度减半+分数衰减
3. **消费链路**: stale_days 通过 DataFrame.attrs 在适配器→FusionController 之间自动传递

**验证**: 两文件代码编译通过 ✅

## 展示层补强 (12:21) — 验收后补充3项

根据用户验收报告，补充3项展示层缺失：
1. **`futu_adapter.py`**: 添加 `logger.warning()` — >30天打印 [CRITICAL] 日志，>3天打印 [STALE] 日志，≤3天打印 [FRESH] ✅
2. **`unified_runner.py` `_print_summary`**: 信号摘要头部打印 ⚠️ 数据滞后标的汇总，每只信号行后加 ⚠️滞后 标记 ✅
3. **`reports/reporter.py`**: 日报新增"数据新鲜度告警"表格，列出所有有滞后警告的标的 ✅

4文件全部编译通过 ✅

## 遗留问题修复 (12:39) — GBK + 旧信号文件

**1. `reports/reporter.py` GBK emoji 修复**
- 添加 `sys.stdout.reconfigure(encoding='utf-8')` 兜底
- `print(f'  📄 日报已生成: ...')` → `print(f'  [日报] 已生成: ...')` 双重保险
- 仅那行 print 有 GBK 问题，Markdown 内容（UTF-8 写文件）不受影响

**2. 刷新 HK 信号文件**
- `unified_runner.py --market HK --signal-only` 重新生成
- 00700 close: 旧 $669.7 → 新 $434.6 ✅
- 09988 close: 旧 $177.6 → 新 $123.9 ✅
- 信号文件 `paper_trading/signals/2026-05-27_HK.json` 已覆盖

## 第三批清理 (12:51) — 审计报告剩余问题修复

外部审计报告提出12个遗留问题，实际验证后按优先级处理4项：

**🔴 立即修 (4项)**
1. ✅ `unified_runner.py` L520,527: `⚠️` → `[STALE]` (GBK防崩)
2. ✅ `core/fusion_controller.py` L551,553: `⛔` → `[EXPIRED]`, `⚠️` → `[STALE]` (GBK防崩)
3. ✅ `unified_runner.py` stale作用域bug: 新增 `_stale_flag(sig)` 内联函数，REDUCED/HOLD/SELL段各自独立计算，不再依赖BUY段残留变量
4. ✅ `scanner2/hk_tech_scan.py` L418-419: 删除死变量 `sig_emoji`

**🟡 待处理 (本周)**
- #6 #7 #8: 回测脚本 `autype=None` 改 `qfq` (simulate_trades.py, backtest_09988.py, backtest_01810.py)

**🟢 跳过/误报**
- #5 `us_pipeline.py` 无分页 → 该文件未使用Futu API，误报
- #12 `_archive/` `tmp/` → 已归档，无需处理

## 审计验收 (13:12) — QuantBot 最终确认
- 代码grep验证全部4项修复到位，无残留emoji/旧变量
- 港股重跑验证: exit code 0, 3.1秒, 日报正常生成, 无GBK报错
- 信号对比: 旧数据00700 BUY+32→新数据HOLD-8，确认滞后数据导致信号完全失真
- 06055 VP从+80(above_box旧)变-80(below_box新)，方向反转

## 审计教训 (13:36) — 架构分层理解错误
第二轮审计12条意见被Roy的agent全部驳回，核心错误：**混淆了宏观市场层(market_state=SPY/VIX)和个股信号层(XMM trend=EMA25/90)**
- market_state=BULL + 6/7港股DOWN ≠ 矛盾，是两层各司其职
- "积极布局"是宏观环境建议，不是对每只股票的喊单
- 教训：审计前先理解架构分层，不要用"单品零售"思维分析"批发+零售"分层系统
- 12条审计意见，0条需要今天动手